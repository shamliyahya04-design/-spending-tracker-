# 💰 دليل البيع (الترخيص + صفحة الهبوط)

هذا الدليل يربط نظام الترخيص بصفحة البيع لتحصل على **منتج جاهز للتسويق**.

---

## 1️⃣ توليد أرقام الترخيص للعملاء

عندما يدفع عميل، أنشئ له مفتاح ترخيص فريد بالأداة:

```bash
# مفتاح صالح سنة، بدون تقييد بجهاز (يصلح لأي هاتف)
node scripts/license-tool.mjs gen --name "أحمد علي" --type pro --days 365

# مفتاح مدى الحياة
node scripts/license-tool.mjs gen --name "شركة الأفق" --type lifetime

# مفتاح مقيد بجهاز واحد (أقوى حماية ضد المشاركة)
# العميل يرسل لك "رمز جهازه" من شاشة التفعيل، ثم:
node scripts/license-tool.mjs gen --name "أحمد" --type pro --days 365 --device ABCD1234
```

أرسل المفتاح للعميل → يفتح التطبيق → يدخل المفتاح → يُفعّل. ✅

> 🔑 المفاتيح موقّعة تشفيرياً (Ed25519). لا أحد يستطيع تزوير مفتاح دون مفتاحك الخاص.

### إدارة المفاتيح (مرة واحدة)
- مفتاحك الخاص: `scripts/license-private.json` — **احتفظ به بسرية، لا ترفعه لـ GitHub** (مستثنى تلقائياً).
- المفتاح العمومي مضمن في التطبيق (آمن للنشر).
- لتوليد زوج مفاتيح جديد: `node scripts/license-tool.mjs init` (يُبطل كل المفاتيح السابقة).

---

## 2️⃣ تخصيص صفحة البيع

كل محتوى صفحة الهبوط (`/landing`) يُضبط من ملف واحد:

**`src/lib/sales-config.ts`** — غيّر فيه:
- `whatsapp`: رقم واتسابك (بصيغة دولية، مثال يمن: `967777123456`)
- `email`: بريدك للتواصل
- `prices`: الأسعار بالعملات المختلفة (يمن/سعودي/دولي)
- `paymentLinks`: الصق روابط الدفع من Stripe / Moyasar / Tap (اختياري)
- `features`, `stats`, `faq`: المميزات والإحصائيات والأسئلة الشائعة

> صفحة الهبوط **عامة** (لا تتطلب تفعيلاً)، بينما التطبيق خلف بوابة الترخيص.

### إنشاء روابط الدفع
- **دولي:** [Stripe Payment Links](https://dashboard.stripe.com/payment-links) (بطاقات)
- **السعودية/الخليج:** [Moyasar](https://moyasar.com) أو [Tap](https://tap.company) (مدى، Apple Pay)
- الصق الرابط في `paymentLinks` وستظهر أزرار الدفع تلقائياً في الصفحة.

---

## 3️⃣ تدفق البيع الكامل

```
العميل يرى صفحة /landing
        ↓
يضغط "اطلب عبر واتساب" (أو يدفع عبر رابط الدفع)
        ↓
تتلقى الطلب ← تؤكد الدفع
        ↓
تولّد مفتاح ترخيص ← ترسل له الملف (APK) + المفتاح
        ↓
يثبّت التطبيق ← يدخل المفتاح ← يُفعّل ← يستمتع 🎉
```

---

## 4️⃣ نموذج التجربة المجانية (Trial)

افتراضياً يعمل التطبيق **7 أيام تجريبية** عند أول فتح، ثم يطلب التفعيل. هذا يزيد نسبة الشراء لأن العميل يجرب القيمة أولاً.

للتعديل: `src/lib/license-config.ts`
```ts
export const LICENSE_CONFIG = {
  required: true,    // true = التطبيق مقفل بعد التجربة
  trialDays: 7,      // مدة التجربة
};
```

لإيقاف القفل أثناء التطوير: اضبط `required: false`.

---

## 5️⃣ ملخص الأوامر

```bash
# الترخيص
node scripts/license-tool.mjs init                    # إنشاء المفاتيح (مرة)
node scripts/license-tool.mjs gen --name ".." --days 365   # توليد مفتاح

# البناء
npm run apk:debug                                    # بناء APK
npm run dev                                          # معاينة محلية
```

---

## 🔒 مستوى الحماية

| الحماية | الحالة |
|---------|--------|
| منع تثبيت التطبيق دون مفتاح صالح | ✅ مفعّل |
| منع تزوير المفاتيح (توقيع تشفيري) | ✅ مفعّل |
| انتهاء صلاحية المفاتيص | ✅ (حسب --days) |
| تقييد المفتاح بجهاز واحد | ✅ (مع --device) |
| منع النسخ بين العملاء | ✅ إذا استخدمت التقييد بالجهاز |

> ملاحظة تقنية: أي تطبيق محلي يمكن لمهندس محترف "اختراقه" نظرياً، لكن بالنسبة لعملاء عاديين يبيعون/يشترون تطبيقاً مالياً، نظام التوقيع هذا حماية قوية وواقعية. للدفاع الأقوى مستقبلاً يمكن إضافة تحقق سحابي عبر خادم.
