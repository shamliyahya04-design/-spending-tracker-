# 📱 دليل بناء وبيع تطبيق الأندرويد (APK)

هذا الدليل يشرح كيف تحوّل المشروع إلى **تطبيق أندرويد (APK)** وتبيعه للعملاء محلياً ودولياً.

> التقنية: **Capacitor** يغلّف تطبيق الويب (Next.js) داخل تطبيق أندرويد حقيقي يعمل **بلا إنترنت وبلا خادم**. كل مستخدم يثبّته على هاتفه وتُحفظ بياناته محلياً — مثالي للبيع المباشر.

---

## 1️⃣ المتطلبات (مرة واحدة)

| الأداة | الغرض | الرابط |
|--------|-------|--------|
| **Node.js 18+** | تشغيل أوامر المشروع | [nodejs.org](https://nodejs.org) |
| **Android Studio** | بناء الـ APK + محاكي | [developer.android.com/studio](https://developer.android.com/studio) |

> Android Studio يتضمّن تلقائياً: JDK + Android SDK. لا تحتاج تثبيتها منفصلة.
> **بديل بدون Android Studio:** استخدم بناء GitHub Actions (القسم 8) — يبني الـ APK في السحابة.

---

## 2️⃣ بناء الـ APK — خطوة بخطوة

```bash
# 1. فك ضغط المشروع وادخل مجلده
cd spending-tracker

# 2. ثبّت الاعتماديات
npm install

# 3. أنشئ منصة أندرويد (مرة واحدة فقط)
npm run android:add

# 4. ابنِ الـ APK للتجربة (نسخة Debug)
npm run apk:debug
```

بعد الانتهاء، ستجد ملف الـ APK هنا:

```
android/app/build/outputs/apk/debug/app-debug.apk
```

✅ **انسخ هذا الملف إلى هاتفك وثبّته** (اسمح "تثبيت من مصادر غير معروفة"). هذا ما تبيعه للعملاء.

> **ملاحظة:** نسخة Debug جيدة للتجربة. للبيع الرسمي استخدم نسخة مُوقّعة (القسم 5).

---

## 3️⃣ توليد أيقونات التطبيق (احترافي)

الأيقونة المصدرية موجودة في `assets/icon.png`. لتوليد كل أحجام الأندرويد تلقائياً:

```bash
npm run assets:generate
```

هذا يولّد الأيقونات الصحيحة داخل `android/app/src/main/res/`. كرّر الأمر بعد كل تعديل للأيقونة.

---

## 4️⃣ تعديل محتوى التطبيق ثم إعادة البناء

أي تعديل في الكود → أعد بناء الويب ثم انسخه إلى أندرويد:

```bash
npm run cap:sync        # يبني الويب + ينسخه لمجلد android
# ثم ابنِ الـ APK مجدداً:
cd android && ./gradlew assembleDebug
```

---

## 5️⃣ توقيع التطبيق (للإصدار النهائي للبيع)

نسخة Release يجب أن تكون موقّعة بمفتاحك حتى يقبلها المتجر وتُحدّث لاحقاً:

```bash
# 1. أنشئ مفتاح التوقيع (مرة واحدة فقط — احتفظ به بسرية!)
keytool -genkey -v -keystore release.keystore \
  -alias spending-tracker -keyalg RSA -keysize 2048 -validity 10000

# 2. أضف بيانات المفتاح إلى android/key.properties
#    (انظر التعليمات الكاملة في توثيق Capacitor: Signing Android Apps)

# 3. ابنِ نسخة الإصدار الموقّعة
npm run apk:release
```

النتيجة: `android/app/build/outputs/apk/release/app-release.apk`

---

## 6️⃣ 💰 البيع المباشر للسوق المحلي (يمن/خليج)

بما أن التطبيق يعمل بلا خادم، يمكنك **بيع ملف APK مباشرة**:

1. ابنِ نسخة موقّعة (`app-release.apk`)
2. أنشئ صفحة هبوط بسيطة (أو استخدم واتساب/إنستغرام) لعرض التطبيق
3. طرق الدفع المحلية:
   - **تحويل بنكي / محفظة (جوال.money، بنكك)**
   - **الدفع عند الاستلام** (تُرسل الملف بعد تأكيد الدفع)
   - **بوابات الخليج:** Moyasar أو Tap أو PayTabs (تدعم مدى/فيزا/Apple Pay)
4. بعد الدفع، أرسل رابط تحميل الـ APK (Google Drive / رابط مباشر)
5. أرشد العميل لتفعيل "تثبيت من مصادر غير معروفة"

> نصيحة: ضع رقماً/رابط دعم للمساعدة في التثبيت — يبني الثقة.

---

## 7️⃣ 🌍 النشر على Google Play كتطبيق مدفوع (سوق دولي)

1. سجّل **حساب مطوّر Google Play** (دفعة واحدة **25$ مدى الحياة**): [play.google.com/console](https://play.google.com/console)
2. ابنِ **ملف AAB** (نسخة المتجر): `cd android && ./gradlew bundleRelease`
   - الناتج: `android/app/build/outputs/bundle/release/app-release.aab`
3. أنشئ تطبيقاً جديداً في Console وارفع ملف الـ AAB
4. اضبط **السعر** في "Pricing" (Google يتقاضى عمولة 15% للأسعار المنخفضة)
5. أضف شاشات العرض والوصف والتصنيف العمري → انشر

> Google Play يتكفّل بالدفع الدولي (فيزا/ماستركارد/حسابات Google) والتحديثات التلقائية.

---

## 8️⃣ ☁️ بناء سحابي بدون Android Studio (مجاني)

لا تريد تثبيت Android Studio؟ ابنِ الـ APK تلقائياً عبر **GitHub Actions**:

1. ارفع المشروع إلى GitHub (تأكد أن `assets/icon.png` موجود)
2. أضف ملف workflow في `.github/workflows/build-apk.yml` (قابل للتوليد عند الطلب)
3. عند كل دفعة (push)، يُبنى الـ APK تلقائياً وتنزّله من تبويب **Actions**
4. الخدمة مجانية ضمن حدود GitHub

> هذه الطريقة مثالية لو جهازك ضعيف — البناء يتم على خوادم GitHub.

---

## 9️⃣ 📊 استراتيجية التسعير المقترحة

| السوق | النموذج | السعر المقترح |
|-------|---------|---------------|
| **محلي (يمن)** | بيع APK لمرة واحدة + دعم | 2,000–5,000 ﷼ |
| **خليج** | بيع APK أو اشتراك سنوي | 15–30 ر.س |
| **دولي** | تطبيق مدفوع على Google Play | 4.99–9.99$ |
| **خادم/مؤسسات** | نسخة مخصّصة بعلامتهم | سعر تفاوضي |

**توصية:** ابدأ ببيع الـ APK مباشرة (أسرع دخول بدون رسوم أو انتظار مراجعة المتجر)، ثم وسّع لـ Google Play بعد تأكيد الطلب.

---

## 🔒 ملاحظات أمنية للبيع

- البيانات محفوظة محلياً على كل هاتف (خصوصية كاملة للعميل).
- **لمنع نسخ الـ APK بسهولة بين المستخدمين:** أضف لاحقاً تفعيل برقم ترخيص (license key) أو ربط بمعرّف الجهاز — يمكنني إضافته عند الحاجة.
- احتفظ بـ `release.keystore` في مكان آمن — بدونه لا تستطيع تحديث التطبيق لاحقاً.

---

## 🆘 أوامر سريعة مرجعية

```bash
npm install              # تثبيت
npm run dev              # معاينة في المتصفح
npm run build            # بناء الويب فقط (مجلد out/)
npm run android:add      # إنشاء مشروع أندرويد (مرة)
npm run assets:generate  # توليد الأيقونات
npm run cap:sync         # نسخ الويب للأندرويد
npm run apk:debug        # بناء APK تجريبي
npm run apk:release      # بناء APK نهائي
cd android && ./gradlew bundleRelease   # بناء AAB للمتجر
```

هل تريد أن أضيف لك **تفعيل برقم ترخيخ**، أو **صفحة هبوط جاهزة للبيع**، أو **ملف GitHub Actions** الجاهز للبناء السحابي؟
